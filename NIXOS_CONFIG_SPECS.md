# NixOS Config Project Specifications

This document records the requirements and specifications for this repository's NixOS configuration.
It serves as a reference for future maintenance and extension.

## General Purpose

Provide a usage experience similar to Fedora Silverblue or the Bluefin project, with:

- A lean, declarative base system via Nix Flakes
- GUI applications installed preferentially via Nix; Flatpak only for apps with no nixpkgs equivalent
- An ephemeral system (preservation) with a clean root on every boot
- Important data preserved in dedicated Btrfs subvolumes

## Composition Architecture

- The flake uses flake-parts with a dendritic pattern.
- Every non-entrypoint Nix module relevant to high-level composition lives under `dendritic/`.
- The host and user inventory is centralized in:
  - `dendritic/data/hosts.nix`
  - `dendritic/data/users.nix`
- The flake outputs are generated by dedicated modules:
  - `dendritic/flake/nixos-configurations.nix` — generates `nixosConfigurations.<host>`
  - `dendritic/flake/disko-configurations.nix` — generates `diskoConfigurations.<host>`
  - `dendritic/flake/home-nixos-module.nix` — wires Home Manager as a NixOS
    module for every user, applied together with `nixos-rebuild switch`
  - `dendritic/flake/home-configurations.nix` — also generates a standalone
    `homeConfigurations."<user>@<host>"` output per user/host pair, deployable
    independently via `home-manager switch` (both paths share one definition,
    `home/mkUserHome.nix`, so they can't drift apart)

## Disk Partitioning

### Requirements

- UEFI support
- Full-disk encryption via LUKS + LVM
- Declarative partitioning via nix-community/disko
- Btrfs filesystem for the main partition
- Hybrid swap: zram (performance) + disk swap (hibernation)
- Ephemeral system via nix-community/preservation

### Partition Layout

```text
Disk (e.g. /dev/nvme0n1)
├── Partition 1: ESP (512 MB, FAT32)
│   └── Mountpoint: /boot
└── Partition 2: LUKS (rest of the disk)
    └── LVM VG: root_vg
        ├── LV swap: 20 GB
        │   └── Swap (for hibernation)
        └── LV root: 100%FREE
            └── Btrfs
```

### Ephemeral root (tmpfs)

The system root (`/`) is a **tmpfs** — an entirely RAM-backed filesystem. This means:

- It's always clean on every boot (no accumulated data, no rollback needed)
- Any file written to `/` is lost on reboot (except what's preserved under `/persist`)
- Simple and reliable: no snapshot, rollback or initrd configuration required

**Mount options:** `defaults,size=50%,mode=755`

### Btrfs subvolumes

The `@` convention is compatible with tools like Timeshift and widely adopted by the community.

| Subvolume | Mountpoint | Characteristics |
| --------- | ---------- | --------------- |
| `@home` | `/home` | Preserved — user directories |
| `@nix` | `/nix` | Preserved — Nix store (essential) |
| `@persist` | `/persist` | Preserved — persistent system data (preservation) |
| `@log` | `/var/log` | Preserved — system logs (no compression) |
| `@containers` | `/var/lib/containers` | Preserved — container data |
| `@flatpak` | `/var/lib/flatpak` | Preserved — Flatpak apps |
| `@snapshots` | `/.snapshots` | Preserved — Btrfs snapshots for backup |

**Global mount options:**

- `compress=zstd` (except `@log`: no compression)
- `noatime`

**Preservation strategy:**

- The tmpfs root is always "clean" on boot — no snapshot or rollback needed
- Important files are preserved in `/persist` via bind mounts (preservation)
- `/persist` is a Btrfs subvolume persistent across boots

### Hybrid Swap

**Per host (16 GB RAM):**

| Component | Size | Priority | Purpose |
| ---------- | ------- | ---------- | -------- |
| zram (zstd) | 8 GB (50% RAM) | 100 (primary) | Everyday performance |
| Disk swap | 20 GB | 5 (backup) | Hibernation |

**Behavior:**

1. The system uses zram first (faster, doesn't wear the SSD)
2. When zram is exhausted, it uses disk swap
3. Hibernation uses disk swap

## Hosts

### barbudus (Dell Inspiron 14 5490)

- **CPU**: Intel i5-10210U
- **RAM**: 16 GB DDR4
- **GPU**: Intel UHD 620 (integrated) + NVIDIA GeForce MX230 (discrete)
- **Storage**: NVMe SSD
- **Special characteristics**:
  - Proprietary NVIDIA drivers (580.x) with PRIME offload
  - Secure Boot via Limine (`boot.loader.limine.secureBoot`)
  - Goodix fingerprint sensor (infinytum libfprint fork)
  - goodix-fp-dump scripts for diagnostics
  - Power management: power-profiles-daemon + thermald

### bigodon (Morefine M6)

- **CPU**: Intel N200
- **RAM**: 16 GB DDR4
- **GPU**: Intel UHD Graphics (integrated, Jasper Lake)
- **Storage**: NVMe SSD
- **Special characteristics**:
  - Intel Xe driver for the modern GPU
  - VA-API with intel-media-driver (iHD)

## Environment Configuration

### Shell

- Available shells: Bash, Fish, Zsh
- Default shell for new users: **Fish**
- Prompt: Starship (cross-shell)

### Editors

- Available editors: Neovim, Helix
- Default system editor: **Neovim** (`$EDITOR=nvim`)

### Boot

- Boot manager: **Limine** (both hosts; barbudus with Secure Boot)
- Splash screen: Plymouth
- Flicker-free setup: `quiet splash` + KMS in the initrd
- Boot menu generation limit: 10

### Printing

- Epson ESC-P/R driver version 1 (`epson-escpr`) - compatible with the L4160
- Epson ESC-P/R driver version 2 (`epson-escpr2`) - newer models
- `ecbd` service enabled by default
- Avahi for network printer discovery

## Graphical Environment

### Noctalia v5

- The Noctalia suite is the only desktop; there's no per-host variant selection. It's
  applied unconditionally to every host's `dendritic.nixos.sharedModules` by
  `dendritic/flake/noctalia-wrapper.nix`.
- **Compositor**: Umbriel — an independent wlroots-based Wayland compositor
  (`programs.umbriel`).
- **Shell**: Noctalia — bars, launcher, dock, notifications, OSDs, lock screen,
  session actions (`programs.noctalia`), no Qt/GTK dependency.
- **Greeter**: Noctalia Greeter, via greetd — replaces GDM (`programs.noctalia-greeter`).
- Per-user compositor/shell settings (keybinds, theme, dock) live in
  `home/users/<user>/noctalia.nix`.
- Default apps are installed via Nix (Ghostty, Bitwarden's SSH agent wiring, etc.) or
  Homebrew (see below); Flatpak covers browsers and a handful of GUI utilities.

### Homebrew (Linuxbrew)

- Available system-wide, similar to Flatpak: a shared prefix at
  `/home/linuxbrew/.linuxbrew`, group-writable by every normal user (`linuxbrew`
  group, added by `users/mkUser.nix`).
- Set up by `modules/system/tools/homebrew.nix` (shared prefix directory via
  `systemd.tmpfiles.rules`, extra `nix-ld` libraries for Electron/GTK casks) and
  `modules/home/apps/homebrew.nix` (per-user bootstrap + declarative Brewfile,
  applied via a `systemd --user` service — Homebrew's installer refuses to run as
  root, so this can't be a system-level service the way Flatpak's is).
- Includes the `ublue-os/homebrew-tap` tap and the `bbrew` TUI.
- A few apps that update very frequently upstream are installed via Homebrew
  instead of nixpkgs (VS Code, `claude-code`, `copilot-cli`, `opencode`) — see the
  Brewfile in `modules/home/apps/homebrew.nix` for the full, current list.

### Flatpak

- System-wide, passwordless installs for users in the `wheel` group
- Configured via a polkit rule (Silverblue-compatible)
- Repository: Flathub (configured automatically)
- Managed declaratively via `nix-flatpak` (the `gmodena/nix-flatpak` input)

**Apps installed declaratively** (`dendritic/flake/noctalia-wrapper.nix` — check
that file for the current, authoritative list; it changes more often than this
document):

| App | Flatpak ID |
|-----|-----------|
| Bitwarden | `com.bitwarden.desktop` |
| Flatseal | `com.github.tchx84.Flatseal` |
| DistroShelf | `com.ranfdev.DistroShelf` |
| Ignition | `io.github.flattool.Ignition` |
| Warehouse | `io.github.flattool.Warehouse` |
| Firefox | `org.mozilla.firefox` |
| Brave | `com.brave.Browser` |
| Zoom | `us.zoom.Zoom` |

**Flatpaks recommended for manual installation:**

```bash
flatpak install flathub org.gnome.Papers   # GNOME: PDF viewer
flatpak install flathub org.kde.okular     # KDE: PDF viewer
flatpak install flathub org.kde.konsole    # KDE: terminal
```

### Browser

- **Brave** installed via Nix (system-wide)
- Set as the default browser via xdg-mime
- Available to all system users

### Fonts

- Noto Fonts (sans, serif, CJK, emoji)
- Liberation TTF
- JetBrains Mono Nerd Font (default monospace)
- FiraCode Nerd Font

## Containers

### Podman (rootless)

- Rootless mode enabled (no root for basic operations)
- `docker` alias available for compatibility
- DNS enabled between containers in compose
- Weekly auto-cleanup of unused images

### Distrobox

- Lets you run any Linux distribution in rootless containers
- Integrated with the user environment (shared home directory)
- Compatible with rootless Podman

## Home Manager

- Used as a NixOS module (`home-manager.nixosModules.home-manager`), integrated into `nixosConfigurations`
- Shares the same system nixpkgs (`useGlobalPkgs = true`), inheriting the overlay and allowUnfree
- User packages installed in `/etc/profiles/per-user/<user>` (`useUserPackages = true`)
- Base configuration in `home/common.nix`
- Per-user configuration in `home/users/<user>/home.nix` (when needed)
- Automatic wiring in `dendritic/flake/home-nixos-module.nix` from the dendritic inventory

## Preservation (Ephemeral System)

### How It Works

1. The system root (`/`) is a **tmpfs** — a RAM-backed filesystem, always empty on boot
2. No rollback, snapshot or initrd service is needed
3. Important files and directories are preserved via bind mounts from `/persist`
4. `/persist` is a persistent Btrfs subvolume (survives any number of reboots)
5. `/home`, `/nix`, `/var/log`, `/var/lib/containers` and `/var/lib/flatpak` are also persistent Btrfs subvolumes
6. Implemented via `nix-community/preservation` (the `preservation.preserveAt."/persist"` module)

### Automatically Preserved Data (System)

- `/etc/nixos` - NixOS configuration
- `/etc/NetworkManager/system-connections` - Network connections
- `/var/lib/systemd` - systemd state
- `/var/lib/nixos` - NixOS internal state
- `/var/lib/bluetooth` - Bluetooth devices
- `/var/db/sudo` - sudo timestamps
- `/etc/machine-id` - Unique machine ID
- Server SSH keys (configured in ssh.nix)

### Per-User Data

None of it needs explicit preservation entries: `/home` is its own persistent
Btrfs subvolume (`@home`, see the table above), not part of the ephemeral tmpfs
root. Whatever a user's session or Home Manager writes under `$HOME` — dotfiles,
`~/.ssh`, `~/.gnupg`, browser profiles, shell history, and so on — survives
reboots on its own. (This wasn't always the case: an earlier iteration of this
repo ran `/home` as tmpfs too, with a hand-maintained list of bind-mounted
per-user paths in `users/mkUser.nix`. That was reverted — see the git history
of `disko.nix`/`modules/system/core/preservation.nix` for context.)

## Security

### LUKS

- AES-256 encryption by default
- `allowDiscards = true` for better performance on SSDs
- Password prompt at boot via Plymouth as a fallback

### TPM2 (Automatic LUKS Unlock)

- Module: `modules/system/security/tpm2.nix`
- Enabled by default on all hosts
- Uses `systemd-cryptenroll` to bind the LUKS key to the TPM2
- PCRs checked: 0 (UEFI firmware) + 2 (UEFI option code) + 7 (Secure Boot state)
- Automatic fallback to manual password if the PCRs change
- Included tools: `tpm2-tools`, `tpm2-tss`
- Kernel modules loaded in the initrd: `tpm_tis`, `tpm_crb`
- **Requires manual enrollment after installation** (see INSTALLATION.md)

### Secure Boot (barbudus)

- Implemented via Limine (`boot.loader.limine.secureBoot`), without shim/MOK
- PKI keys stored in `/persist/etc/secureboot` (symlinked from `/var/lib/sbctl`)
- The firmware verifies the signature of the Limine binary; kernel/initrd are
  verified via a BLAKE2B checksum embedded in the enrolled `limine.conf`

### SSH

- Password authentication disabled by default
- Root login disabled
- Server keys at `/persist/etc/ssh/`

### FIDO2/U2F (YubiKey)

- Module: `modules/system/security/yubikey.nix`
- Opt-in via `security.fido2Auth.enable` (default **off** on every host) — when
  enabled, adds `pam_u2f` to sudo, run0, TTY login, the graphical greeter and
  polkit/pkexec, each falling back to password if there's no valid entry in
  `/persist/etc/u2f-mappings`
- PC/SC (smartcard access), fingerprint PAM wiring and the secret-service
  keyring are independent of this flag and stay on regardless
- `pkgs/yubikey-gpg-import` / `scripts/import-gpg-yubikey.sh`: import and trust
  a YubiKey's OpenPGP public key (the latter is the live-ISO/pre-install
  variant, needed before the system — and this flake's packages — exist)

## References and Inspirations

- [Fedora Silverblue](https://silverblue.fedoraproject.org/)
- [Project Bluefin](https://projectbluefin.io/)
- [Erase Your Darlings (ephemeral system)](https://grahamc.com/blog/erase-your-darlings/)
- [NixOS Preservation](https://github.com/nix-community/preservation)
- [Disko](https://github.com/nix-community/disko)
- [Btrfs on NixOS](https://nixos.wiki/wiki/Btrfs)
- [Arch Wiki — Btrfs](https://wiki.archlinux.org/title/Btrfs)
- [Limine Bootloader](https://github.com/limine-bootloader/limine)
- [Old repository](https://github.com/lbssousa/nixos-config-old)
